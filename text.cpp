#include <iostream>
#include <string>
#ifdef _WIN32
#include <windows.h>
#endif
using namespace std;

void setup_console_encoding() {
#ifdef _WIN32
    SetConsoleOutputCP(CP_UTF8);
    SetConsoleCP(CP_UTF8);
#endif
}

class student {
private:
    int id;
    static int totalCount;  // ????????
    string name;

public:
    // ========== ???? ==========
    // ??????
    student() : id(0), name("???") {
        totalCount++;  // ???????,????1
    }

    // ???????
    student(int studentId, string Name)
        : id(studentId), name(Name) {
        totalCount++;  // ???????,????1
    }

    // ??????
    student(const student& other)
        : id(other.id), name(other.name) {
        totalCount++;  // ???????????
    }

    // ????
    ~student() {
        totalCount--;  // ?????,????1
    }

    // ========== ?????? ==========
    // ??????,?????
    // ????,????????totalCount
    static int getCount() {
        return totalCount;
    }

    // ========== ?????? ==========
    // ??????
    void display() {
        cout << "??: " << id << ", ??: " << name << endl;
    }

    // ????
    void set_id(int new_id) {
        id = new_id;
    }

    // ????
    void set_name(string new_name) {
        name = new_name;
    }

    // ????
    int get_id() {
        return id;
    }

    // ????
    string get_name() {
        return name;
    }
};

// ========== ???????? ==========
int student::totalCount = 0;

int main() {
    setup_console_encoding();
    cout << "========== ?????????? ==========" << endl;

    // ????
    cout << "?????: " << student::getCount() << endl;

    // ???????
    student a;  // ????????
    cout << "\n????a?:" << endl;
    a.display();
    cout << "?????: " << student::getCount() << endl;

    // ???????
    student b(1001, "??");
    cout << "\n????b?:" << endl;
    b.display();
    cout << "?????: " << student::getCount() << endl;

    // ???????
    student c(1002, "??");
    cout << "\n????c?:" << endl;
    c.display();
    cout << "?????: " << student::getCount() << endl;

    // ???????????
    cout << "\n???????????:" << endl;
    cout << "??????: student::getCount() = " << student::getCount() << endl;
    cout << "????a??: a.getCount() = " << a.getCount() << endl;
    cout << "????b??: b.getCount() = " << b.getCount() << endl;
    cout << "????c??: c.getCount() = " << c.getCount() << endl;
    cout << "??????????,??????????" << endl;

    // ????????
    cout << "\n????????:" << endl;
    student d = b;  // ????????
    d.set_id(1003);
    d.set_name("?????");
    d.display();
    cout << "????????: " << student::getCount() << endl;

    // ????????
    cout << "\n????public????private??:" << endl;
    cout << "???:" << endl;
    b.display();

    b.set_id(9999);
    b.set_name("??????");

    cout << "???:" << endl;
    b.display();

    // ????public????private??
    cout << "\n????public????private??:" << endl;
    cout << "b???: " << b.get_id() << endl;
    cout << "b???: " << b.get_name() << endl;

    // ??????
    cout << "\n??????:" << endl;
    student* p = new student(1004, "??");
    p->display();
    cout << "????????: " << student::getCount() << endl;

    delete p;
    cout << "??????????: " << student::getCount() << endl;

    // ?????
    cout << "\n?????:" << endl;
    {
        student e(1005, "??");
        cout << "?????????: " << student::getCount() << endl;
    }  // e??????
    cout << "???????????: " << student::getCount() << endl;

    cout << "\n========== ???? ==========" << endl;
    cout << "?????: " << student::getCount() << endl;

    return 0;
}
